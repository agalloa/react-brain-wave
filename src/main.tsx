import React from 'react'
import ReactDOM from 'react-dom/client'

import { ReactOpenIA } from './ReactOpenIA'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ReactOpenIA />
  </React.StrictMode>,
)
